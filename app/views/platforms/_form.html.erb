<div class="form-horizontal" role="form">
  <%= form_for(@platform) do |f| %>
      <legend>Legend</legend>
     <div class="form-group">
      <% if @platform.errors.any? %>
    	<div id="error_explanation">
      	<h2><%= pluralize(@platform.errors.count, "error") %> prohibited this platform from being saved:</h2>

      	<ul>
      		<% @platform.errors.full_messages.each do |message| %>
        	  <li><%= message %></li>
      		<% end %>
	</ul>

      </div>
      <% end %>

      <div class="form-group">
        <%= f.label :PLATFORM_NO, class: "col-lg-3 control-label" %>
        <div class="col-lg-3">
          <%= f.number_field :PLATFORM_NO ,class: "form-control" %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :PLATFORM_NAME, class: "col-lg-3 control-label" %>
        <div class="col-lg-3">
          <%= f.text_field :PLATFORM_NAME ,class: "form-control" %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :PLATFORM_NAME_DISP, class: "col-lg-3 control-label" %>
        <div class="col-lg-3">
          <%= f.text_field :PLATFORM_NAME_DISP ,class: "form-control" %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :PLATFORM_SIMPLENAME_DISP, class: "col-lg-3 control-label" %>
